# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。本檔只放交接必需的精簡資訊，詳細脈絡放 Obsidian（若有 L3）。

## ⏯️ 目前做到哪
已套用 **Claude Design 茶館風設計稿**（專案 `5d7d41d9-41a1-4437-9c91-340231fadca0`，設計系統「黃三郎 Tea House」）並部署上線。社區名稱定為 **聯懋超綻**。

設計稿同時改了營運規則，皆已實作：
- 時段由 3 段（上午/下午/晚間）改為 **08:00–22:00 每 2 小時共 7 段**
- 預約改為**四步驟精靈**：選場地 → 週曆選時段 → 填資料 → 完成
- 新增**「同一時段每戶限預約一項設施」跨場地限制**（`unitSlotHolds` 佔位文件）
- 新增 **14 天預約範圍**限制
- 表單欄位改為門牌／姓名／電話（移除人數），加規則同意勾選
- 查詢碼格式改為 **YX-8F2K6**，輸入時自動正規化（小寫、漏連字號都可查）
- 容納人數依設計稿調整（棋藝 8、KTV 10、健身 12、親子 15、宴會廳 60、SPA 6）
- 新增社區公告區（含後台公告管理模組）、預約規則區、印章品牌標記、頁尾
- 後台改為側欄版面，統計改為今日預約／使用率／今日取消／暫停開放場地

## 🚦 目前狀態
**住戶端已在 production 完整驗證通過**：
- 六場地載入、公告顯示、49 格週曆（7 時段 × 7 天）
- 完成預約 → 查詢碼 `HE-4DWMK`（格式正確）
- **跨場地同時段規則實測擋下**：同門牌同時段改訂健身室 → 正確拒絕
- 小寫無連字號查詢碼可正規化查到；取消後時段與跨場地佔位皆自動釋出
- 手機 375px 零水平溢出、週曆可橫向捲動、控制台零錯誤

**物業後台程式碼完成但仍未實測登入後功能**（Agent 依安全規則不能替使用者輸入密碼）。

## ➡️ 下一步
1. **使用者自行登入 `/admin` 點測後台**，特別是新的「公告管理」與「設備管理」
2. 補上三項設計稿標為待補的內容：
   - **場地實景照片**（目前是「場地照片」文字佔位，設計稿註明「請提供實景照片」）
   - **LINE 通知**（設計稿標為示意；需社區的 LINE 官方帳號與 Messaging API，目前未實作）
   - 頁尾服務時間（目前 shared.js 有 `serviceHours` 但頁尾未顯示）
3. 考慮把 GitHub repo 改為 Private

## ⚠️ 注意事項
- 專案資料夾在 Google 雲端硬碟（`G:\我的雲端硬碟\AI\Reservation`），換電腦前確認同步完成
- Firebase 專案 `reservation-98067`（Firestore 位於 asia-east1），由 shingyong02@gmail.com 擁有，jnfakimo 為協作擁有者
- **社區名稱與電話集中在 `public/js/shared.js` 的 `COMMUNITY`**；HTML 裡的 `.contact-phone` 會在載入時被 JS 覆寫，只改 HTML 沒用
- **公告與後台預約篩選需要複合索引**，已寫進 `firestore.indexes.json` 並隨 `firebase deploy --only firestore` 一起部署。缺索引時公告區會靜默隱藏
- billing 未啟用（Spark 方案），架構刻意不依賴 Cloud Functions
- 次數上限在免登入前提下無法真正強制（換門牌即可繞過），理由見 `docs/optimization-report.md` 第三節
- 本地測試：背景執行 `python -m http.server 8899`（cwd 為 `public/`），再用 `.claude/launch.json` 的 `reservation` 設定 attach

## 🕐 最後更新
- 時間：2026-08-06 15:10
- 更新者：Claude Code @ DESKTOP-0CFB6UK
- Git push：待推送
